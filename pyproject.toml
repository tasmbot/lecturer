[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "lecturer"
version = "0.1.0"
description = "Local lecture recording, transcription and summarization pipeline"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]