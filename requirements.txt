# Захват аудио
sounddevice>=0.4.6
numpy>=1.26

# Транскрибация
faster-whisper>=1.0.0

# Суммаризация (клиент Ollama через HTTP)
requests>=2.31

# Экспорт в Anytype (MCP-клиент)
mcp>=1.0.0

# Опционально — чтение .env вместо переменных окружения
python-dotenv>=1.0
